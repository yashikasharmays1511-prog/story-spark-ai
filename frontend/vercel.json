{
  "installCommand": "cd .. && corepack enable && pnpm install",
  "buildCommand": "pnpm --filter story-spark-ai-frontend run build",
  "outputDirectory": "dist",
  "rewrites": [{ "source": "/(.*)", "destination": "/" }]
}
